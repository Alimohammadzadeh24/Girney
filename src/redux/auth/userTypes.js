//create user actions type for change and set information in state 
const userActionTypes = {
    SET_NUMBER: 'SET_NUMBER',
    SET_ORIGIN: 'SET_ORIGIN',
    SET_DESTINATION: 'SET_DESTINATION'
}
//create user actions type for change and set information in state 

export default userActionTypes;